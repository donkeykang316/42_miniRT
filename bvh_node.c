#include "minirt.h"

/*bool    box_compare(t_object_list *obj_list, int a, int b, int axis)
{
    t_aabb  box_a;
    t_aabb  box_b;

    if (!bounding_box(obj_list->value.sphere[a], &box_a) || !bounding_box(object_l->value.sphere[b], &box_b))
        ft_putstr_fd("No bounding box in bvh_node constructor.\n", 1);
    return (box_a.min.e[axis] < box_b.min.e[axis]);
}*/
